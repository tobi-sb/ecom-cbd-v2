'use client';

// Debug wrapper component is no longer using any imports
// Removed unused imports: dynamic, DebugLogger

export default function ClientDebugWrapper() {
  // Return null to remove the debug button from the website
  return null;
}
